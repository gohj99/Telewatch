<resources>
    <!-- Base application theme. -->
    <style name="Theme.Telewatch" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.Telewatch.Splash" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowContentOverlay">@drawable/ic_launcher_foreground</item>
    </style>
</resources>